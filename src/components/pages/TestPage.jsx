const TestPage = () => {

   return (
      <p style={{ margin: "50px auto", fontSize: "80px" }}>Hello</p>
      /* <button onClick={() => setId(548111)}> get char by id </button> */
      //   <ul>
      /* {data?.results.map((item) => (
            <li key={item.id}>
               <p style={{ margin: "auto", fontSize: "25px" }}>{item.name}</p>
            </li>
         ))} */
      //   </ul>
   );
};

export default TestPage;
